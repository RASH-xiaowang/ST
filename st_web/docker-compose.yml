# 本地一键部署：docker compose up -d --build
# 访问 http://localhost:8080

services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: "${NEXT_PUBLIC_SITE_URL:-https://harness.dev}"
    ports:
      - "8080:80"
    restart: unless-stopped
